 R←Test_Misc_02(stopFlag batchFlag);⎕IO;⎕ML;⎕TRAP;parms;rc;msg;ref
⍝ Send an email with APL characters
 ⎕IO←1 ⋄ ⎕ML←1
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 parms←⎕NS''
 parms.To←∆SERVER.Userid
 parms.CC←∆TO
 parms.Subj←'From Dyalog SMTP: ',⊃⎕SI
 parms.Body←'APL characters: Prim←{⍵/⍨2=+⌿0=⍵∘.|⍵}'
 parms.Body,←⎕UCS 13 10
 (rc msg ref)←∆SERVER ##.SendMail parms
 Assert 0=rc

 :If ∆ConfirmEmailOkay'(watch out for APL characters)'
     R←T._OK
 :EndIf
⍝Done
